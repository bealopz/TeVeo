/**
 * Gemini model for generating text-based story outlines and JSON structures.
 */
export const TEXT_GENERATION_MODEL = 'gemini-3-flash-preview';

/**
 * Gemini model for generating images from text prompts.
 */
export const IMAGE_GENERATION_MODEL = 'gemini-2.5-flash-image';

/**
 * Base URL for placeholder images (e.g., loading states).
 */
export const PLACEHOLDER_IMAGE_URL = 'https://picsum.photos/500/500';

/**
 * Panel width for the stitched comic strip.
 */
export const PANEL_WIDTH = 500;

/**
 * Panel height for the stitched comic strip.
 */
export const PANEL_HEIGHT = 500;

/**
 * Height reserved for captions below each panel.
 */
export const CAPTION_HEIGHT = 50;

/**
 * Padding between comic panels and around the entire strip.
 */
export const COMIC_PADDING = 20;

/**
 * Default number of panels to generate for a comic.
 */
export const DEFAULT_PANEL_COUNT = 4;